import javax.swing.*;

public class SistemasNumericos {
    public static void main(String[] args) {
        /*//hacer un input
        String numeroDecimal = JOptionPane.showInputDialog(null, "numero entero");
        //trata de ejecutar el codigo
        int convertir = 0;

        System.exit(0);//para terminar como STOP

        try{
           convertir  = Integer.parseInt(numeroDecimal);
        }catch (NumberFormatException s){
            JOptionPane.showMessageDialog(null,"ERORRR");
            main(args);
            System.exit(0);//para terminar como STOP

        }

        System.out.println("numero = " + convertir);*/
/*
        String nombre = "Camilo";
        String nombre2 = new String("Camilo");

        System.out.println(nombre == nombre2); //en un string verifica si son el mismo objeto

        System.out.println(nombre.equalsIgnoreCase(nombre2)); //verifica por valor

        System.out.println(nombre.length());
        //string valor inmutable, que no cambia
        // transformar el valor de un string
        String concatenar = nombre.transform(n -> {
            return n + " y " + "Camila";
        });
        System.out.println("concatenar = " + concatenar);

        //cambiar un caracter en un string
        String reemplaxar = concatenar.replace("C","c");
        System.out.println("reemplaxar = " + reemplaxar);

        StringBuilder sb = new StringBuilder(a);
        //Probar tiempo en java
        long inicio = System.currentTimeMillis();//tiempo en milisegundos
        //for pequeño
        for(int i = 0; i< 500; i++){

        };//for para ser un contador se segundos


        long fin = System.currentTimeMillis();

        System.out.println(fin - inicio);*/


    }
}
